This library only contains class objects that define the entity the system will use.  There is no logic in here. 

Only classes with properties, enums and can then have interfaces that defines an object.